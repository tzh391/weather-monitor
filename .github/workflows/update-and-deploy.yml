name: Collect Weather Data

on:
  schedule:
    # 每5分钟运行一次
    - cron: '*/5 * * * *'
  workflow_dispatch:  # 允许手动触发
  push:
    branches:
      - main
    paths:
      - 'dataCollector.js'
      - '.github/workflows/collect-data.yml'

# 设置权限
permissions:
  contents: write  # 允许写入和推送

jobs:
  collect:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: 📂 Create directories
        run: |
          mkdir -p weather_data
          mkdir -p public
          ls -la
          echo "Current directory: $(pwd)"
          echo "weather_data exists: $(test -d weather_data && echo 'yes' || echo 'no')"
      
      - name: 🌤️ Collect weather data
        run: |
          echo "=========================================="
          echo "Starting data collection..."
          echo "=========================================="
          node dataCollector.js
          echo "=========================================="
          echo "Data collection completed"
          echo "=========================================="
      
      - name: 📊 Check generated files
        run: |
          echo "=========================================="
          echo "Checking weather_data directory..."
          echo "=========================================="
          ls -lah weather_data/ || echo "weather_data directory is empty or doesn't exist"
          echo ""
          echo "File count:"
          find weather_data -name "*.csv" | wc -l
          echo ""
          echo "CSV files:"
          find weather_data -name "*.csv" -exec echo "  - {}" \; -exec wc -l {} \;
          echo ""
          echo "Sample content (first 5 lines of first file):"
          find weather_data -name "*.csv" -type f | head -1 | xargs head -5 || echo "No CSV files found"
      
      - name: 📈 Generate web page
        run: |
          echo "=========================================="
          echo "Generating web page..."
          echo "=========================================="
          node generateWebPage.js
          echo "=========================================="
          echo "Web page generation completed"
          echo "=========================================="
      
      - name: 📊 Check output files
        run: |
          echo "=========================================="
          echo "Checking public directory..."
          echo "=========================================="
          ls -lah public/ || echo "public directory is empty or doesn't exist"
          echo ""
          if [ -f public/weather_data.json ]; then
            echo "weather_data.json size: $(wc -c < public/weather_data.json) bytes"
            echo "First 500 characters:"
            head -c 500 public/weather_data.json
          else
            echo "weather_data.json not found"
          fi
      
      - name: 🔍 Debug - Show all files
        if: always()
        run: |
          echo "=========================================="
          echo "All files in workspace:"
          echo "=========================================="
          find . -type f -name "*.csv" -o -name "*.json" | head -20
      
      - name: 📤 Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 添加所有数据文件
          git add weather_data/*.csv || echo "No CSV files to add"
          git add public/weather_data.json || echo "No JSON file to add"
          git add public/index.html || echo "No HTML file to add"
          
          # 检查是否有更改
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            echo "Committing changes..."
            git commit -m "🌤️ Update weather data - $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
            git push
            echo "Changes pushed successfully"
          fi
      
      - name: 📊 Upload artifacts (for debugging)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: weather-data-${{ github.run_number }}
          path: |
            weather_data/
            public/
          retention-days: 7
